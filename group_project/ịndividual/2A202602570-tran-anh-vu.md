# Individual contribution report

## Thông tin

- Họ và tên: Trần Anh Vũ
- Mã học viên: 2A202602570
- Nhóm: 2ae
- Repository/branch: K4-L3A-RAG-Pipeline-2ae / `main` (chatbot RAG tuyển sinh Đại học FPT)

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Task 1 — Thu thập tài liệu legal | Tải thủ công 3 đề án tuyển sinh FPT 2022/2023/2024 (PDF) vào `data/landing/legal/`; viết `SOURCES` + kiểm tra chữ ký `%PDF-`, chỉ tải lại file thiếu/hỏng | `src/task1_collect_legal_docs.py`; commit `ddca626`, `37b8e98` | Done |
| Task 4 — Chunking, embedding, indexing | Đọc Markdown, chunk recursive (500/50), embed `BAAI/bge-m3` (cosine, normalize), upsert ChromaDB với ID ổn định `<file>::chunk-<i>` | `src/task4_chunking_indexing.py`; commit `37b8e98` | Done |
| Task 5 — Semantic search | Embed query bằng chính `embed_texts()` của Task 4, đổi cosine distance thành score, khử trùng ID, sort giảm dần, `retrieval_method="dense"` | `src/task5_semantic_search.py`; commit `37b8e98` | Done |
| Task 7 — RRF | `sum(1/(k+rank))`, rank từ 1, mỗi list chỉ tính một lần cho mỗi ID, đánh dấu `hybrid` | `src/task7_reranking.py`; commit `37b8e98` | Done |
| Task 9 — Retrieval pipeline | Dense + BM25 → RRF đúng một lần; so threshold với cosine gốc của dense; PageIndex lỗi thì trả hybrid, không crash | `src/task9_retrieval_pipeline.py`; commit `37b8e98` | Done |
| Golden dataset | 23 câu (20 in-domain gồm học phí, hạn đăng ký, ký túc xá, quy chế 2026, học bổng, 1 câu multi-hop; 3 câu out-of-domain). Mọi `expected_context` được script kiểm tra là trích nguyên văn từ `data/standardized/` | `group_project/evaluation/golden_dataset.json`; commit `3b72d5c` | Done |
| Chuẩn hóa NFC + `SCORE_THRESHOLD` từ `.env` | Thêm NFC vào Task 3/4, xóa chunk cũ khi index lại, `retrieve` đọc ngưỡng từ `.env` | `task3_convert_markdown.py`, `task4_chunking_indexing.py`, `task9_retrieval_pipeline.py` | Partial — sửa xong nhưng chưa commit, chưa index lại |

## Quyết định kỹ thuật quan trọng

1. **Quyết định:** Chunk `RecursiveCharacterTextSplitter` (500 ký tự, overlap 50) và dùng ID ổn định + upsert (kèm xóa chunk cũ không còn tồn tại).  
   **Lý do/evidence:** 8 tài liệu cho 4445 chunks; chạy lại pipeline không tạo bản trùng, khớp yêu cầu "index lại không tạo dữ liệu trùng" trong `docs/MODULE_CONTRACTS.md`; test contract kiểm tra `chunk_index` liên tục và chunk ≤ `CHUNK_SIZE * 1.1`.  
   **Trade-off:** Chunk nhỏ giúp truy xuất chính xác nhưng các bảng dài (học phí, chỉ tiêu) của đề án có thể bị cắt giữa hàng, làm mất ngữ cảnh.

2. **Quyết định:** So sánh threshold fallback với cosine score gốc của dense search, không dùng RRF score.  
   **Lý do/evidence:** RRF score chỉ phản ánh thứ hạng (~0,03) nên không so được với ngưỡng cosine. Khi thử thực tế với ngưỡng mặc định 0,3: câu in-domain có score cao nhất 0,57–0,64, còn câu out-of-domain 0,41–0,63 (câu "học phí Bách khoa Hà Nội" đạt 0,626), nên mọi câu đều vượt 0,3 và fallback không bao giờ chạy.  
   **Trade-off:** Dense score một mình không tách hẳn được câu ngoài domain có từ khóa gần corpus; phần này phải dựa thêm vào safe refusal của bước generation.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng: `pytest tests/test_contracts.py -q` (chữ ký hàm, chunk, semantic search, RRF, retrieve/fallback); `pytest tests/test_acceptance.py -q` (corpus, golden dataset ≥ 15 câu); query thực tế trên ChromaDB (4446 chunks) cho câu in-domain và out-of-domain.
- Kết quả trước/sau nếu có: lần chạy gần nhất 19 pass, 1 fail (`test_evaluation_report_is_completed`, do `RESULT.md` còn TODO — thuộc phần report nhóm). Test golden dataset pass sau khi tạo dataset.
- Lỗi đã phát hiện và cách xử lý:
  - Chroma không nhận giá trị `None` trong metadata (`url`) → lọc `None` trước khi upsert, khôi phục `url=None` khi search để đúng contract.
  - `chunk_index` bị nhảy số khi có chunk rỗng → đánh số sau khi bỏ chunk rỗng.
  - 5/8 file Markdown chứa Unicode dạng NFD (làm lệch BM25/so khớp chuỗi; phát hiện khi kiểm chứng `expected_context` của golden dataset) → thêm chuẩn hóa NFC.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: ngưỡng `SCORE_THRESHOLD` chưa được hiệu chỉnh chính thức (đang là giá trị trong `.env`); các sửa NFC và ngưỡng chưa commit và chưa chạy lại index, nên chưa có số liệu trước/sau. Golden dataset mới có 3 câu out-of-domain.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: chạy lại Task 3 + Task 4 với NFC, chạy `src/calibrate_threshold.py` để chọn ngưỡng, rồi chạy `src/evaluation.py` để có số liệu A/B (dense-only vs hybrid + RRF) làm bằng chứng cho các quyết định trên.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 20/09/2026
- Tên thành viên: Trần Anh Vũ
